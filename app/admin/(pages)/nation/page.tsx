import AdminTemplatePlaceholder from '@/components/admin/sportverse/AdminTemplatePlaceholder';

export default function AdminNationPage() {
    return <AdminTemplatePlaceholder moduleName='Nation module (archived)' description='Nation là module phân loại phục vụ content du lịch. Template CMS hiện không dùng module này làm mặc định để tránh giữ dependency cũ.' />;
}