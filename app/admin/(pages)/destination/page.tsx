import AdminTemplatePlaceholder from '@/components/admin/sportverse/AdminTemplatePlaceholder';

export default function AdminDestinationPage() {
    return <AdminTemplatePlaceholder moduleName='Destination module (archived)' description='Destination là phần domain-specific của bộ travel cũ. Trong template CMS này, module đó đã được rút về placeholder để tránh kéo theo contract legacy không cần thiết.' />;
}
