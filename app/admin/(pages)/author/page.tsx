import AdminTemplatePlaceholder from '@/components/admin/sportverse/AdminTemplatePlaceholder';

export default function AdminAuthorPage() {
    return <AdminTemplatePlaceholder moduleName='Author module (archived)' description='Author management của travel stack cũ không còn nằm trong bộ template CMS mặc định. Template hiện ưu tiên posts, menus, sections, organizations, venues, store và booking.' />;
}
