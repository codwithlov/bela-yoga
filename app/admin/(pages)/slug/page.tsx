import AdminTemplatePlaceholder from '@/components/admin/sportverse/AdminTemplatePlaceholder';

export default function AdminSlugPage() {
    return <AdminTemplatePlaceholder moduleName='Slug manager (archived)' description='Slug manager chi tiết của hệ cũ đã được đưa về placeholder. Template mới dùng slug đơn giản ngay trên post/menu/store và có thể mở rộng sau.' />;
}
