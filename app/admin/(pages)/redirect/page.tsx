import AdminTemplatePlaceholder from '@/components/admin/sportverse/AdminTemplatePlaceholder';

export default function AdminRedirectPage() {
    return <AdminTemplatePlaceholder moduleName='Redirect module (archived)' description='Redirect manager cũ theo SEO/travel vẫn có thể được mang lại khi cần, nhưng không còn là module mặc định của template CMS fullstack này.' />;
}
