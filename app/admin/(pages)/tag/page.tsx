import AdminTemplatePlaceholder from '@/components/admin/sportverse/AdminTemplatePlaceholder';

export default function AdminTagPage() {
    return <AdminTemplatePlaceholder moduleName='Tag module (archived)' description='Tag taxonomy nâng cao của travel stack tạm thời không còn active trong template. Khi dự án thật cần taxonomy sâu hơn, có thể triển khai lại trên model post mới.' />;
}
